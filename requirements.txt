# Core Dependencies
setuptools==80.10.2       # highest release still shipping pkg_resources (removed in 81.0.0), which eth_keyfile/web3 import at runtime; 83.0.0 (the only PYSEC-2026-3447 fix) breaks that import — CVE ignored in CI instead
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

email-validator==2.3.0
# Database
sqlalchemy==2.0.45
alembic==1.12.1
asyncpg==0.29.0
psycopg2-binary==2.9.9
redis==5.0.1

# Async Tasks
celery==5.3.4
kombu==5.3.4

# AWS Services
boto3==1.40.0
botocore==1.40.0
urllib3==2.7.0        # pinned to clear decompression-bomb / redirect CVEs; botocore>=1.35 lifted the <2.1 cap

# Blockchain
web3==6.11.0
eth-account==0.8.0

# PDF & QR Generation
reportlab==4.0.4
qrcode[pil]==7.4.2
Pillow==12.3.0            # bumped from 12.2.0 to clear PYSEC-2026-2253..2257 / 3451..3453
python-docx==1.2.0
beautifulsoup4==4.12.3
lxml==6.1.0

# AI Integration
openai==1.3.0
httpx==0.25.2
stripe==15.1.0

# WebSockets
python-socketio==5.16.2

# Security
python-jose[cryptography]==3.5.0   # bumped from 3.4.0: 3.4.0 capped pyasn1<0.5.0, which pinned us to the vulnerable 0.4.8; 3.5.0 requires pyasn1>=0.5.0 with no upper bound
pyasn1==0.6.4             # transitive (python-jose, rsa) — pinned explicitly to clear CVE-2026-59885/59886 and PYSEC-2026-2263; unblocked by the python-jose 3.5.0 bump above
passlib[bcrypt]==1.7.4
bcrypt>=3.2.0,<4.0.0
cryptography==46.0.7      # bumped from 46.0.5 to clear PYSEC-2026-35/36 (GHSA-537c-gmf6-5ccf needs 48.x major jump — ignored in CI)
jellyfish>=1.0.0          # fuzzy name matching for CSP sanctions screening
defusedxml==0.7.1         # hardened XML parsing for untrusted EU/UN sanctions feeds (csp_sanctions.py)
slowapi==0.1.9
# Enterprise SSO — SAML 2.0 (used by /api/v1/enterprise/sso/saml/*)
pysaml2==7.5.0

# Monitoring
opentelemetry-api==1.21.0
opentelemetry-sdk==1.21.0
prometheus-client==0.19.0
# Error tracking — inert unless SENTRY_DSN is set (see app/main.py)
sentry-sdk[fastapi]==1.45.1   # patch from 1.45.0 clears PYSEC-2026-1917 (stay on 1.x; 2.x is an API break)

# Testing
pytest==7.4.3
pytest-asyncio==0.21.1
pytest-cov==4.1.0
pytest-mock==3.12.0
pypdf==6.14.2
moto[s3,ses]==5.1.4        # bumped from 5.0.0 for boto3/botocore 1.40 compat — 5.0.0's S3 mock truncated uploads from the newer client
freezegun==1.5.1

# Utilities
feedparser==6.0.11
python-dotenv==1.2.2      # bumped from 1.0.0 to clear PYSEC-2026-2270
python-multipart==0.0.31  # bumped from 0.0.30 to clear PYSEC-2026-3040
python-dateutil==2.8.2
greenlet==3.0.1

# Browser automation for screenshots.
# NOTE: playwright is intentionally NOT here — it (and its bundled Node driver)
# lives only in the worker image via requirements-worker.txt, so the public app
# image stays free of the Chromium/Node CVE surface the CI Trivy gate scans.
# The API screenshot path degrades to public providers when it is absent
# (see app/services/screenshot_service.py).
django-ckeditor==6.7.3
mangum==0.17.0
openpyxl==3.1.5
prometheus-fastapi-instrumentator==6.1.0
